name: PiDashboard
options:
  bundleIdPrefix: technology.blackbelt.pidashboard
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
  groupSortPosition: top

packages:
  PiDashboardKit:
    path: ../PiDashboardKit
  MarkdownUI:
    url: https://github.com/gonzalezreal/swift-markdown-ui
    from: "2.4.0"

settings:
  base:
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "3"
    SWIFT_VERSION: "6.0"
    DEVELOPMENT_TEAM: "ZPD66G9CB6"
    CODE_SIGN_STYLE: Automatic
    CODE_SIGNING_REQUIRED: "YES"
    CODE_SIGNING_ALLOWED: "YES"

targets:
  PiDashboard:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: Sources
    dependencies:
      - package: PiDashboardKit
        product: PiDashboardKit
      - package: MarkdownUI
        product: MarkdownUI
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: technology.blackbelt.pidashboard
        PRODUCT_NAME: PiDashboard
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        INFOPLIST_KEY_CFBundleDisplayName: "pi dashboard"
        INFOPLIST_KEY_UILaunchScreen_Generation: "YES"
        INFOPLIST_KEY_UISupportedInterfaceOrientations: "UIInterfaceOrientationPortrait"
        INFOPLIST_KEY_UIStatusBarStyle: UIStatusBarStyleLightContent
        TARGETED_DEVICE_FAMILY: "1"
        GENERATE_INFOPLIST_FILE: "YES"
        ENABLE_USER_SCRIPT_SANDBOXING: "YES"
    info:
      path: Sources/Info.plist
      properties:
        CFBundleDisplayName: "pi dashboard"
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
        NSPhotoLibraryUsageDescription: "Attach photos from your library to a pi session message."
        NSMicrophoneUsageDescription: "Record voice to dictate a message to your pi session."
        # App Store Connect asks the export-compliance question on every upload unless
        # this is declared. The app's only cryptography is HTTPS to the dashboard (and
        # the OAuth cookie it carries), which is exempt — so declaring false answers it
        # once, here, instead of by hand at each upload.
        ITSAppUsesNonExemptEncryption: false
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
          NSAllowsLocalNetworking: true
        # ASWebAuthenticationSession OAuth callback scheme (multi-operator cookie gate).
        # xcodegen regenerates Info.plist from here, so the scheme must live in BOTH.
        CFBundleURLTypes:
          - CFBundleTypeRole: Editor
            CFBundleURLName: technology.blackbelt.pidashboard.auth
            CFBundleURLSchemes:
              - pidashboard

  PiDashboardUITests:
    type: bundle.ui-testing
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: UITests
      - path: ../qa-e2e/PiDashboardUITests
    dependencies:
      - target: PiDashboard
      - package: PiDashboardKit
        product: PiDashboardKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: technology.blackbelt.pidashboard.uitests
        GENERATE_INFOPLIST_FILE: "YES"

schemes:
  PiDashboard:
    build:
      targets:
        PiDashboard: all
        PiDashboardUITests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - PiDashboardUITests
